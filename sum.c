#include <stdio.h>
int main() {
    int n, i, sum = 0;

    printf("Enter a positive integer: ");
    scanf("%d", &n);

    printf("Enter a positive integer: ");
    scanf("%d", &i);

    sum = n + i;

    printf("%d", sum);
    return 0;
}
